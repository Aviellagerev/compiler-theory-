/* test implicit/explicit type conversions */

  i, j : int;
 a, b : float;

{
   input (a);
  input (i);

 

   if (a >= i)  {

       b = a - i;
       output(b);
   }
   else {
       output(i * a);
   }



   i = cast<int>(3.0 + 5.8);
   output (i);

   i = cast<int>(3 + 5); /* this is legal */
   output(i);

 

   a = cast<float>(3 + 5);
   output(a);

   a = cast<float> (3.0 + 5.0); /* this is legal*/
   output(a);

   

}